# 后端测试用例清单

本文件列出 `backend_tests.py` 覆盖的主要用例。每个“前端字段”都可以直接复制到浏览器页面测试。

## 1. LaTeX Leibniz 导数预览

前端字段：

| 字段 | 内容 |
|---|---|
| 输入格式 | LaTeX |
| 方程类型 | ODE |
| 求解模式 | 解析解 |
| 方程 | `\frac{d^2 y}{dx^2} + y = 0` |
| 未知函数 | `y` |
| 自变量 | `x` |
| 条件 | 空 |

自动测试：`preview-latex-leibniz`

## 2. 简谐振子解析通解

前端字段：

| 字段 | 内容 |
|---|---|
| 输入格式 | Wolfram |
| 方程类型 | ODE |
| 求解模式 | 解析解 |
| 方程 | `y''[x] + y[x] == 0` |
| 未知函数 | `y` |
| 自变量 | `x` |
| 条件 | 空 |

自动测试：`analytic-harmonic-general`

## 3. 简谐振子初值问题

前端字段：

| 字段 | 内容 |
|---|---|
| 输入格式 | Wolfram |
| 方程类型 | ODE |
| 求解模式 | 解析解 |
| 方程 | `y''[x] + y[x] == 0` |
| 未知函数 | `y` |
| 自变量 | `x` |
| 条件 | `y[0] == 1`<br>`y'[0] == 0` |

自动测试：`analytic-harmonic-ivp`

## 4. 一阶指数增长方程

前端字段：

| 字段 | 内容 |
|---|---|
| 输入格式 | Wolfram |
| 方程类型 | ODE |
| 求解模式 | 解析解 |
| 方程 | `y'[x] == y[x]` |
| 未知函数 | `y` |
| 自变量 | `x` |
| 条件 | 空 |

自动测试：`analytic-exponential-growth`

## 5. 一阶非齐次线性方程

前端字段：

| 字段 | 内容 |
|---|---|
| 输入格式 | Wolfram |
| 方程类型 | ODE |
| 求解模式 | 解析解 |
| 方程 | `y'[x] + y[x] == Exp[-x]` |
| 未知函数 | `y` |
| 自变量 | `x` |
| 条件 | `y[0] == 0` |

自动测试：`analytic-inhomogeneous-first-order`

## 6. 二维线性 ODE 组解析解

前端字段：

| 字段 | 内容 |
|---|---|
| 输入格式 | Wolfram |
| 方程类型 | ODE 组 |
| 求解模式 | 解析解 |
| 方程 | `x'[t] == y[t]`<br>`y'[t] == -x[t]` |
| 未知函数 | `x, y` |
| 自变量 | `t` |
| 条件 | `x[0] == 1`<br>`y[0] == 0` |

自动测试：`analytic-linear-system`

## 7. Logistic 方程数值解

前端字段：

| 字段 | 内容 |
|---|---|
| 输入格式 | Wolfram |
| 方程类型 | ODE |
| 求解模式 | 数值解 |
| 方程 | `y'[t] == 2 y[t] (1 - y[t])` |
| 未知函数 | `y` |
| 自变量 | `t` |
| 条件 | `y[0] == 0.1` |
| 绘图区间 | `{t, 0, 5}` |
| 绘图 | 可不勾选 |

自动测试：`numeric-logistic`

## 8. 简谐 ODE 组数值解 + 绘图

前端字段：

| 字段 | 内容 |
|---|---|
| 输入格式 | Wolfram |
| 方程类型 | ODE 组 |
| 求解模式 | 数值解 |
| 方程 | `x'[t] == y[t]`<br>`y'[t] == -x[t]` |
| 未知函数 | `x, y` |
| 自变量 | `t` |
| 条件 | `x[0] == 1`<br>`y[0] == 0` |
| 绘图区间 | `{t, 0, 2 Pi}` |
| 绘图 | 勾选 |

自动测试：`numeric-system-plot`

## 9. Van der Pol 非线性方程数值解

前端字段：

| 字段 | 内容 |
|---|---|
| 输入格式 | Wolfram |
| 方程类型 | ODE |
| 求解模式 | 数值解 |
| 方程 | `x''[t] - (1 - x[t]^2) x'[t] + x[t] == 0` |
| 未知函数 | `x` |
| 自变量 | `t` |
| 条件 | `x[0] == 2`<br>`x'[0] == 0` |
| 绘图区间 | `{t, 0, 10}` |
| 绘图 | 可勾选 |

自动测试：`numeric-vanderpol`

## 10. 阻尼受迫振子数值解

前端字段：

| 字段 | 内容 |
|---|---|
| 输入格式 | Wolfram |
| 方程类型 | ODE |
| 求解模式 | 数值解 |
| 方程 | `x''[t] + 0.2 x'[t] + x[t] == Sin[1.5 t]` |
| 未知函数 | `x` |
| 自变量 | `t` |
| 条件 | `x[0] == 0`<br>`x'[0] == 0` |
| 绘图区间 | `{t, 0, 20}` |
| 绘图 | 可勾选 |

自动测试：`numeric-damped-forced`

## 11. 热方程 PDE 数值解 + 3D 图

前端字段：

| 字段 | 内容 |
|---|---|
| 输入格式 | Wolfram |
| 方程类型 | PDE |
| 求解模式 | 数值解 |
| 方程 | `D[u[x,t],t] == D[u[x,t],{x,2}]` |
| 未知函数 | `u` |
| 自变量 | `x, t` |
| 条件 | `u[x,0] == Sin[x]`<br>`u[0,t] == 0`<br>`u[Pi,t] == 0` |
| 绘图区间 | `{x, 0, Pi}, {t, 0, 1}` |
| 绘图 | 勾选 |

自动测试：`numeric-heat-pde-plot`，默认跳过，需 `--include-slow`

## 12. 错误输入：单等号

前端字段：

| 字段 | 内容 |
|---|---|
| 输入格式 | Wolfram |
| 方程类型 | ODE |
| 求解模式 | 解析解 |
| 方程 | `y'[x] = y[x]` |
| 未知函数 | `y` |
| 自变量 | `x` |

预期：提示 Wolfram 方程应使用 `==`。

自动测试：`validation-bare-equals`

## 13. 错误输入：圆括号函数

前端字段：

| 字段 | 内容 |
|---|---|
| 输入格式 | Wolfram |
| 方程类型 | ODE |
| 求解模式 | 解析解 |
| 方程 | `y(x) == x` |
| 未知函数 | `y` |
| 自变量 | `x` |

预期：提示 Wolfram 函数应用应写作 `y[x]`。

自动测试：`validation-parentheses-warning`

## 14. 安全拦截：系统进程调用

前端字段：

| 字段 | 内容 |
|---|---|
| 输入格式 | Wolfram |
| 方程类型 | ODE |
| 求解模式 | 解析解 |
| 方程 | `RunProcess[{"echo","bad"}] == 0` |
| 未知函数 | `y` |
| 自变量 | `x` |

预期：后端拒绝危险 Wolfram 调用。

自动测试：`security-runprocess`

## 15. 安全拦截：非法函数名

前端字段：

| 字段 | 内容 |
|---|---|
| 输入格式 | Wolfram |
| 方程类型 | ODE |
| 求解模式 | 解析解 |
| 方程 | `y[x] == 0` |
| 未知函数 | `y;Run` |
| 自变量 | `x` |

预期：后端拒绝非法标识符。

自动测试：`security-invalid-identifier`

## 16. 安全拦截：绘图区间复合表达式

前端字段：

| 字段 | 内容 |
|---|---|
| 输入格式 | Wolfram |
| 方程类型 | ODE |
| 求解模式 | 数值解 |
| 方程 | `y'[x] == y[x]` |
| 未知函数 | `y` |
| 自变量 | `x` |
| 条件 | `y[0] == 1` |
| 绘图区间 | `{x, 0, 10}; Run["bad"]` |

预期：后端拒绝带分号的复合表达式。

自动测试：`security-plot-range-compound-expression`

## 17. Wolfram 消息回传：欠定方程组

前端字段：

| 字段 | 内容 |
|---|---|
| 输入格式 | Wolfram |
| 方程类型 | ODE 组 |
| 求解模式 | 数值解 |
| 方程 | `x'[t] == y[t]` |
| 未知函数 | `x, y` |
| 自变量 | `t` |
| 条件 | `x[0] == 1` |
| 绘图区间 | `{t, 0, 1}` |

预期：求解失败，并在提示中回传 `NDSolve::underdet`。

自动测试：`messages-underdetermined-system`
